# File Objects

f = open('test.txt', 'r')  # 'r' lets just read file, 'w' lets write to the file, 'a' let's append to file, 'r+' lets ready and write to file

print(f.name)  # print name of the file

f.close()  # <-- if we open the file, we need to close the file here



